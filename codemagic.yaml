workflows:
  android-native-build:
    name: Android Native Build
    environment:
      android_sdk: default
      java: 17
    scripts:
      - name: Build file APK (Debug)
        script: |
          gradle assembleDebug
    artifacts:
      - build/outputs/apk/**/*.apk
      
